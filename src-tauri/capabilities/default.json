{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the local lore archive window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
